name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
#        os: [macos-latest, windows-latest]
        os: [windows-latest]

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    
    steps:
    - name: Check out code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Clean install dependencies
      run: |
        npm ci --prefer-offline
      shell: bash

    - name: Build for Windows (on Windows runner)
      if: matrix.os == 'windows-latest'
      run: |
        npm run electron:make -- --arch=x64
      shell: cmd
      timeout-minutes: 20

    - name: Build for macOS (on macOS runner)
      if: matrix.os == 'macos-latest'
      run: npm run electron:make
      timeout-minutes: 20

    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: release-${{ matrix.os }}
        path: out/
        retention-days: 5

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    timeout-minutes: 10
    permissions:
      contents: write

    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4

    - name: Display structure of downloaded files
      run: ls -R

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          release-windows-latest/out/make/**/*
          release-macos-latest/out/make/**/*.dmg
          release-macos-latest/out/make/**/*.zip
        draft: true
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}